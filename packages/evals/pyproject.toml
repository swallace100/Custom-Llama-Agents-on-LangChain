[project]
name = "evals"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = ["pytest", "ruff", "mypy", "black"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
